class todoItem{
    constructor(title, desc, due, prio, checklist) {
        this.title = title;
        this.desc = desc;
        this.due = due;
        this.prio = prio;
        this.checklist = checklist;
    }
}

export { todoItem }

